<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Only used for the window background shown before Compose renders; all real UI theming is done in ui/theme/Theme.kt -->
    <style name="Theme.PixelInfo" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Shown by MainActivity only, via installSplashScreen(); hands off to Theme.PixelInfo once the first frame is ready. -->
    <style name="Theme.PixelInfo.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.PixelInfo</item>
    </style>
</resources>
